
/**
 * Returns true if the given character is a whitespace character.
 * 
 * @param c The character to check.
 * @return True if the given character is a whitespace character.
 */
public func [[inline]] is_space(c: char) bool {
    return c == ' ' || c == '\t' 
       || c == '\n' || c == '\r';
}
/**
 * Returns true if the given character is an
 *  upper-case character.
 *
 * @param c The character to check.
 * @return True if the given character is an
 *  upper-case character.
 */
public func [[inline]] is_upper(c: char) bool 
    { return c >= 'A' && c <= 'Z'; }
/**
 * Returns true if the given character is a
 *  lower-case character.
 *
 * @param c The character to check.
 * @return True if the given character is a
 *  lower-case character.
 */
public func [[inline]] is_lower(c: char) bool
    { return c >= 'a' && c <= 'z'; }
/**
 * Returns true if the given character is a
 *  digit.
 *
 * @param c The character to check.
 * @return True if the given character is a
 *  digit.
 */
public func [[inline]] is_digit(c: char) bool
    { return c >= '0' && c <= '9'; }
/**
 * Returns true if the given character is a
 *  hexadecimal digit.
 *
 * @param c The character to check.
 * @return True if the given character is a
 *  hexadecimal digit.
 */
public func [[inline]] is_hex(c: char) bool { 
    return is_digit(c) || (c >= 'a' && c <= 'f') 
      || (c >= 'A' && c <= 'F'); 
}
/**
 * Returns true if the given character is an
 *  octal digit.
 *
 * @param c The character to check.
 * @return True if the given character is an
 *  octal digit.
 */
public func [[inline]] is_oct(c: char) bool
    { return c >= '0' && c <= '7'; }
/**
 * Returns true if the given character is a
 *  letter.
 *
 * @param c The character to check.
 * @return True if the given character is a
 *  letter.
 */
public func [[inline]] is_alpha(c: char) bool
    { return is_upper(c) || is_lower(c); }
/**
 * Returns true if the given character is a
 *  letter or digit.
 *
 * @param c The character to check.
 * @return True if the given character is a
 *  letter or digit.
 */
public func [[inline]] is_alpha_num(c: char) bool
    { return is_alpha(c) || is_digit(c); }
/**
 * Returns true if the given character is a
 *  control character.
 *
 * @param c The character to check.
 * @return True if the given character is a
 *  control character.
 */
public func [[inline]] is_control(c: char) bool
    { return c < 32 || c == 127; }
/**
 * Returns true if the given character is a
 *  printable character.
 *
 * @param c The character to check.
 * @return True if the given character is a
 *  printable character.
 */
public func [[inline]] is_print(c: char) bool
    { return !is_control(c); }
/**
 * Returns true if the given character is a
 *  punctuation character.
 *
 * @param c The character to check.
 * @return True if the given character is a
 *  punctuation character.
 */
public func [[inline]] is_punc(c: char) bool {
    return c == '!' || c == '"' || c == '#' 
      || c == '$' || c == '%' || c == '&' 
      || c == '\'' || c == '(' || c == ')' 
      || c == '*' || c == '+' || c == ',' 
      || c == '-' || c == '.' || c == '/' 
      || c == ':' || c == ';' || c == '<' 
      || c == '=' || c == '>' || c == '?' 
      || c == '@' || c == '[' || c == '\\' 
      || c == ']' || c == '^' || c == '_' 
      || c == '`' || c == '{' || c == '|' 
      || c == '}' || c == '~';
}
/**
 * Returns true if the given character is a
 *  symbol character.
 *
 * @param c The character to check.
 * @return True if the given character is a
 *  symbol character.
 */
public func [[inline]] is_sym(c: char) bool {
    return c == '+' || c == '-' || c == '*' 
      || c == '/' || c == '\\' || c == '|' 
      || c == '&' || c == '^' || c == '<' 
      || c == '>' || c == '=' || c == '~' 
      || c == '@' || c == '$' || c == '%' 
      || c == '!' || c == '?';
}
/**
 * Returns true if the given character is a
 *  mark character.
 *
 * @param c The character to check.
 * @return True if the given character is a
 *  mark character.
 */
public func [[inline]] is_mark(c: char) bool {
    return c == '`' || c == '\'' || c == '^' 
      || c == '"' || c == '~' || c == '.' 
      || c == '-' || c == '_' || c == '*' 
      || c == '+' || c == '=' || c == '<' 
      || c == '>' || c == '@' || c == ':' 
      || c == '/' || c == '\\' || c == '|' 
      || c == '!' || c == '?' || c == '#' 
      || c == '$' || c == '%' || c == '&' 
      || c == '(' || c == ')' || c == '[' 
      || c == ']' || c == '{' || c == '}';
}

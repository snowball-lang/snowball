use Core::System;

namespace tests {

fn [[test]] empty() i32 {
    let v = new Vector<i32>();
    return v.size() == 0;
}

fn [[test(expect = 5)]] push() i32 {
    let mut v = new Vector<i32>();
    let mut i = 0;
    while (i < 5) {
        v.push(i);
        i = i + 1;
    }
    return v.size();
}

}

use Core::CLib

class Parent: X {
  pub:
    let a: i32;
    // TODO: typecheck this and generate it in llvm
    Parent() : a(254) {}
}

pub fn main() i32 {
    let p = new Parent();
    CLib::putchar(p.a as char);
}
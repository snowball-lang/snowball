import System
import Math

class Child {
    priv var d: Int = 34
    pub operator new() {

    }

    pub operator +(n2: i32) Int {
        return self.d + n2
    }
}

fn main() Int {
    var a = new Child()
    var b = a + 6
    var c = Math.sin(b as f64)

    var e = Math.inf + c
    if e > 100 {
        System.println("This works?")
    }

}

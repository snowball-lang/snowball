use Core::CLib

class A {
  pub:
    A() {
    }

    // TODO: also test for 'x: f32' <- some funny things going on :)
    fn hello() {
      CLib::putchar('a')
    }    
}

class B: A {
  pub:
    B() {
    }

    // TODO: replace this with A::hello if it is not declared
}

pub fn main() i32 {
    let b = new B();

    b.hello(2);
}

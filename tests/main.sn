use Core::CLib

class A {
  pub:
    A() {
    }

    fn hello() {
      CLib::putchar('a')
    }    
}

class B: A {
  pub:
    B() {
    }

    // TODO: replace this with A::hello if it is not declared
}

pub fn main() i32 {
    let b = new B();
    let a = new A();

    a.hello(2);
    b.hello(2);
}

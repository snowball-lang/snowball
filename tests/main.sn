import System

fn fib(n: i64) i64 {
    if n <= 1 {
        return n
    }

    return fib(n-1) + fib(n-2)
}

fn main() Int {
    // TODO: output number is wrong!?!?!??!
    System.println(fib(47 as i64))
}


import System
test "perform additions" skip /* skip until floats work*/ {
    var a = 2
    var b = 6
    var c = 2.6

    var d = a + 4
    var e = b + c

    assert c == b
    assert e == 8.6
}

test "check if numbers are the same" {
    assert 2 == 2
    assert 10000000000000 == 10000000000000
    assert 0b0100 == 0b0100
    assert 0xFFFFF == 0xFFFFF
}

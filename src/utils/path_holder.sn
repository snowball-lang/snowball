
import std::fs::{Path};
import pkg::compiler::global_ctx::{CachedPath};

public class PathHolder {
    let path: CachedPath;
    
    public: PathHolder(path: CachedPath) : path(path) {}
    
    @inline func get_path() CachedPath { return self.path; }
}

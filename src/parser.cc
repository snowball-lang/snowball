
#include "parser.h"



namespace snowball {

    void Parser::parse() {
        // while (true) {
        //     printf("L: %s\n", (*_current_token).to_string().c_str());
        //     _current_token++;
        // }
    }
}
